[output]
dir = "./showcase"
formats = ["png"]

[[scenes]]
type = "terminal"
command = "python -c \"from src.bootstrap import bootstrap; print('Lepus Classifier ready'); print('CNN-based rabbit species classifier'); print('Training images: ~85')\" 2>&1"
name = "training-run"
theme = "dracula"
cols = 100
maxLines = 40
